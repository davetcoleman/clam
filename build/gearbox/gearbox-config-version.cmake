set( PACKAGE_VERSION 10.11.0 )

if( "${PACKAGE_FIND_VERSION_MAJOR}" EQUAL 10 )
if( "${PACKAGE_FIND_VERSION_MINOR}" EQUAL 11 )
    set( PACKAGE_VERSION_COMPATIBLE 1 )
    set( PACKAGE_VERSION_EXACT 1 )
endif()
endif()
